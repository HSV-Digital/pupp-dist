# Asset Notice

This repository includes bundled binary assets used by the application at runtime, including:

- Proposal PowerPoint templates under `apps/web/public/proposal_templates`
- Proposal and GTM assets under `apps/api/assets` and `apps/api/static`
- Segoe UI font assets under `apps/web/public/segoeui` and `apps/api/assets/fonts`

Review your commercial rights before redistributing or replacing any bundled asset pack.

## Bundled notices

- Segoe UI font notice: `apps/web/public/segoeui/EULA.txt`

## Operational note

If you replace bundled assets, keep the relative paths stable or update the corresponding storage and asset-path configuration.
